# hello-piton

The finished specification from the Piton tutorial of the same name.

    piton build

That writes the agent instructions into `.claude/` and `src/`. Then start
Claude Code in this directory and ask it to build the application.

Needs the `piton` binary on your PATH:
https://github.com/piton-lang/piton-rs
